import { Plugin } from 'ckeditor5/src/core';
// import { buildDefinition, isSupported } from './utils';
// import AdCalloutCommand from './adcalloutcommand';

export default class AdCalloutEditing extends Plugin {
    init() {
        console.log( 'AdCalloutEditing#init() got called' );
    }
}